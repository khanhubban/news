name: news_app
description: "A modern, cross-platform news application."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Core Functionality
  flutter_riverpod: ^2.5.1
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.4
  firebase_messaging: ^14.9.3
  dio: ^5.4.3+1
  go_router: ^14.2.0
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.2.3

  # UI & UX
  youtube_player_flutter: ^9.0.1
  webview_flutter: ^4.8.0
  shimmer: ^3.0.0
  cached_network_image: ^3.3.1
  share_plus: ^9.0.0
  intl: ^0.19.0

  # Utilities
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

